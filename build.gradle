subprojects {
    apply plugin: 'java'
}

project(':use_cases') {
    dependencies {
        compile project(':core'), project(':web-logic')
    }
}

project(':web-logic') {
    dependencies {
        compile project(':core')
    }
}

project(':web') {
    dependencies {
         compile project(':core'), project(':use_cases'), project(':web-logic')
    }
}
